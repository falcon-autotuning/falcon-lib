autotuner UnaryTest (int value, bool flag) -> (int negated, bool inverted) {
  negated = 0;
  inverted = false;
  
  start -> compute;
  
  state compute {
    negated = -value;
    inverted = !flag;
    -> done;
  }
  
  state done {
    terminal;
  }
}
