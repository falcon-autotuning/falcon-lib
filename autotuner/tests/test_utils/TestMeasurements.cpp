#include "TestMeasurements.hpp"

// Implementations are in the header (all inline)

namespace falcon::autotuner::test {

// Empty file - implementations in header

} // namespace falcon::autotuner::test
