
autotuner StateWrite() -> (bool completed) {
  bool completed = false;

  start -> init;
  
  state init {
    // state reads should happen only to predefined variables
    write(scope="globals", name="completed", extra="default", value=completed);
    -> done;
  }
  
  state done {
    terminal;
  }
}
