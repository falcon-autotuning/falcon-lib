autotuner UnaryTest (int value, bool flag) -> (int negated, bool inverted) {
  int negated = 0;
  bool inverted = false;
  
  start -> compute;
  
  state compute {
    negated = -value;
    inverted = !flag;
    -> done;
  }
  
  state done {
    terminal;
  }
}
