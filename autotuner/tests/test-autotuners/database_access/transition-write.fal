import "io";
autotuner TransitionWrite -> (bool completed) {
  completed = true;
  Error err = nil;
  err = io::write(scope="device", name="completed", extra="default", characteristic=completed);
  start -> init;
  
  state init {
    terminal;
  }
  
}
