import "io";
autotuner StateWrite() -> (bool completed) {
  completed = false;

  start -> init;
  
  state init {
    // state reads should happen only to predefined variables
    io::write(scope="globals", name="completed", extra="default", characteristic=completed);
    -> done;
  }
  
  state done {
    terminal;
  }
}
