import io;
autotuner TransitionRead -> (bool completed) {
  completed = false;
  Error err = nil;
  completed, err = io::readLatest(scope="device", name="completed", extra="default");

  start -> init;
  
  state init {
    -> done;
  }
  
  state done {
    terminal;
  }
}
