autotuner TransitionRead -> (bool completed) {
  completed = false;
  Error err = nil;
  completed, err = readLatest(scope="device", name="completed", extra="default");

  start -> init;
  
  state init {
    -> done;
  }
  
  state done {
    terminal;
  }
}
