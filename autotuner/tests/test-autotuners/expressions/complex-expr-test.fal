autotuner ComplexExprTest (int a, int b, int c) -> (int result) {
  result = 0;
  
  start -> compute;
  
  state compute {
    result = (a + b) * c - 10;
    -> done;
  }
  
  state done {
    terminal;
  }
}
