autotuner ArithmeticTest (float x, float y) -> (float add, float sub, float mul, float div) {
  add = 0.0;
  sub = 0.0;
  mul = 0.0;
  div = 0.0;
  
  start -> calculate;
  
  state calculate {
    add = x + y;
    sub = x - y;
    mul = x * y;
    div = x / y;
    -> done;
  }
  
  state done {
    terminal;
  }
}
