#pragma once

#include <string_view>

namespace falcon::database::embedded {

constexpr std::string_view snapshot_schema =
    R"FALCONSCHEMA(@SNAPSHOT_SCHEMA_CONTENT@)FALCONSCHEMA";

} // namespace falcon::database::embedded
